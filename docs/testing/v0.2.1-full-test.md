# Arelis capability walk — ledger

Live install is **0.2.2** at `%LOCALAPPDATA%\Programs\Arelis`. Dev checkout is
this repo (`pythonw -m arelis`, data in this repo’s `data\`). Close live
Arelis first if both would want port 8765.

This file is the **17–18 Aug 2026** walk that produced 0.2.2. It is not a
score out of 80; each id is a real step. Re-run a line only if a later fix
touches that area.

When something is wrong: thinking rail (`Ctrl+1`), then `logs\arelis.log`,
`logs\voice.log`, `logs\turns.jsonl`.

Destructive steps (real mail / texts / calendar): undo immediately. Send to
yourself or someone you have warned.

Do not write real Gmail, names, LAN IPs, or home paths into tracked files.

---

## Where we are (18 Aug 2026, night)

**0.2.2 is shipped.** Glass tabs/lists (3.1 / 3.4), cmd-flash hide, and the
browser cluster below were the last packing items; they are in that release.

**This checkout is the daily-driver try, not a release.** No version bump, no
installer rebuild. Live 0.2.2 at `%LOCALAPPDATA%\Programs\Arelis` is
untouched. Overlay: `fast` and `research` both `qwen3.5:9b` in
`data/config.local.yaml`. The **code** role is gone here — file/git/debug
stays on `fast` plus workspace/code skills. Notes: [models.md](../models.md),
[architecture.md](../architecture.md), [whats-new.md](../whats-new.md).

**Not walked by a person.** This sitting was pytest, then docs. Nobody has
re-run the ids below on this checkout. Do that before treating it as a
daily driver, and before any release.

The 17–18 Aug walk below was **0.2.2 / Qwen2.5**. It still stands for
features. Re-run a line only if a later fix touches that area.

### This sitting (checkout)

| What | Where it landed |
|------|-----------------|
| Two chips only (`/role fast`, `/role research`) | orchestrator, UI combo, rooms kinds still `code`/`analysis` as folder leans |
| Same-tag research does not unload VRAM | router `same_chat_weights` |
| Cold-launch orbit stayed hidden because `Inbound notify ready` went into chat | thinking dock only — [notify-inbound.md](../notify-inbound.md) |
| Thinking dock stacked one Qwen3.5 token per line | `ThinkingPanel.extend_stream` joins a wrapping **think** paragraph |
| Sherpa heard `emile` / `in box` for mail | `repair_stt_mail_words` + inbox/email skill aliases |
| After a good scrape, empty chat content started **JSON fallback** | finish from last tool result (`empty after tool; answering from result`) |
| History + RECENT both on orbit; docks from last night felt like idle “ending” | Away rest is opt-in (Settings → Window). Launch keeps layout. Ghosts hide when History is showing (`isHidden`) |

### Walked on 9B this evening (spot, not a full re-ledger)

| Ask | Result |
|-----|--------|
| Weather tomorrow | `weather(days=2)` — good |
| New mail / inbox | `inbox(action=list, unread_only=True)` — good after the mail-word repair |
| SpaceX / SPCX quote | scrape hit MarketBeat NASDAQ:SPCX ~$143.34. Training may still say private; the scrape is the warrant. Leftover was JSON fallback after the scrape — gated, not re-walked live yet |

Restart **Arelis (dev)** after pulling these so the process has the gate and the orbit fix.

**Still deferred** (not a packing blocker): table at the bottom, plus a full
9B overlay walk of the ids below.

---

## Browser cluster — walked (18 Aug)

### 12.1 Open — PASS

*"Open YouTube in your browser."* / *"open youtube.com in your browser"*

Allow `browser(action=open)`. Lands on `https://www.youtube.com/` (not leftover
`/results`). Drive strip stays.

### 12.3 Search — PASS

*"Search for interferometry videos and tell me the top three results."*

Allow `browser(action=search, site=youtube)`. Window goes to results. Names
real hits. **No** `web_search`, **no** scrape.

### 12.2 Page text — PASS (recap)

*"What's on this page?"*

Injected `browser(action=read)`. Routing right; she often recaps the last
search list instead of describing the tab (sidebar, Sign in, ads).

### 12.4 Pause / go — PASS

Pause → “Arelis Chrome paused — page stays” with **go** / **stop**. Go
resumed.

### 12.5 Sign in click — PASS

*"go to sign in"* → Allow snapshot, then Allow `click` on the header Sign in
(e.g. `e10`). Chrome went to Google sign-in (email/phone identifier). Strip:
your turn; she does not type the password. Hit **Go** when you are in.

*"go to signin"* (one word) first tried `navigate` to a guessed
`/account_signin` URL — owner Stopped. That guessed navigate is now rewritten
to snapshot before Allow. Re-walk optional.

If you then give a username/email, she may type it into a non-secret field.
**She must not type the password.**

---

## Look after browser — glass (retest after restart)

### 3.1 / 3.4 Glass, not native grey

Open History and Camera (View, or the bottom tab strip). Open Settings (F1),
**Roots**.

**Expect:** history/camera selected tab is amber-on-ember, not a Windows-grey
chip. Roots list is the same dark well as other fields, not a native grey
box. Add / Update / Remove / OK / Cancel match the rest of Settings.

Shipped in **0.2.2** (opaque fills + Fusion). This line was the last packing
blocker on the morning walk.

Settings honesty that already passed: Listen/Speak warn that a restart is
needed; copy-URL on a systems row works. **Test Speak** still says “airelyse”
— later (3.4 note), not this look.

### 6.6 Calculator — PASS (with a note)

*"What is sqrt(2)*pi?"* — real calculator, ~4.44288.

*"What is sqrt(2)pi?"* (no `*`) did not parse. Owner: fine.

### 7.5 OCR — PASS (cmd flash)

Read printed words from a photo. `ocr` ran. A cmd window flashed — same
subprocess console as `/role code`. Process-wide `CREATE_NO_WINDOW` shipped
in **0.2.2**.

---

## Done — do not re-walk

Each line is the step, not a score. Re-run only if a later fix touches that
area.

### 1 Launch


| Id      | What it is                                                                       |
| ------- | -------------------------------------------------------------------------------- |
| **1.1** | Cold launch: orbit, “what are we working on.” New chat, not last night’s thread. |
| **1.3** | Top **systems** readout. Image row says whether ComfyUI is reachable.            |


### 2 Chat / roles


| Id      | What it is                                                                       |
| ------- | -------------------------------------------------------------------------------- |
| **2.1** | Thinking line and parked orbit shimmer for the whole wait. They do not pop.      |
| **2.4** | Stop / Esc kills the turn. Next ask is only the new question.                    |
| **2.5** | `/role` (and `/roll` typo). On **0.2.2**: `/role code` after a stop does not pop a cmd window. This checkout: only `fast` / `research`. |
| **2.6** | `/help` lists commands (`/role`, `/rooms`, …). Not a tool call.                  |


### 3 Window


| Id      | What it is                                                              |
| ------- | ----------------------------------------------------------------------- |
| **3.2** | View menu opens/closes docks.                                           |
| **3.5** | Delete a conversation (history). Glass confirm, not a grey Windows box. |


### 4 Memory


| Id      | What it is                                     |
| ------- | ---------------------------------------------- |
| **4.1** | “Remember that …” — Allow, fact stored.        |
| **4.2** | That fact is in Settings → Memory.             |
| **4.3** | Recall by asking.                              |
| **4.4** | History survives Quit.                         |
| **4.5** | Delete conversation from history.              |
| **4.6** | Forget by asking. One Allow. The fact is gone. |


### 5 Files


| Id             | What it is                                                                                                  |
| -------------- | ----------------------------------------------------------------------------------------------------------- |
| **5.1**        | Read a file under a workspace root.                                                                         |
| **5.2**        | Write a file. Allow. Lands on disk.                                                                         |
| **5.3 / 13.3** | `/room set root` on a real folder. Workspace project dropdown follows that folder, not the Arelis checkout. |


### 6 World


| Id      | What it is                                                       |
| ------- | ---------------------------------------------------------------- |
| **6.1** | Weather for *your* place.                                        |
| **6.2** | Location / where she thinks you are.                             |
| **6.3** | Search the web + scrape a real page (news). Not a browser drive. |
| **6.4** | Fetch a URL.                                                     |
| **6.5** | Research report with sources.                                    |


### 7 Seeing


| Id      | What it is                                                                               |
| ------- | ---------------------------------------------------------------------------------------- |
| **7.1** | Paste an image + “what’s in this image?” → vision, not SMS.                              |
| **7.2** | Paste + YouTube thumbnail / vibrance → `image_edit` on those pixels, not Comfy generate. |
| **7.3** | “Draw …” / generate — Comfy. Parks after.                                                |
| **7.4** | Camera snapshot, then ask what she sees.                                                 |


### 8 Voice (wake)


| Id          | What it is                                                  |
| ----------- | ----------------------------------------------------------- |
| **8.1–8.3** | “Hey Arelis” wakes. Icon latches. Orbit says listening.     |
| **8.4**     | `Ctrl+M` dictate. Speech lands in the composer unsent.      |
| **8.6**     | While she is speaking, start talking. She stops (barge-in). |
| **8.7**     | Say only “Arelis” (no “Hey”). She does not wake.            |


### 9 Mail / SMS


| Id      | What it is                                                                                                                                                                              |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9.1** | Inbox / recent mail.                                                                                                                                                                    |
| **9.2** | Search mail.                                                                                                                                                                            |
| **9.3** | “Email me …” quoted subject/body. Arrives at **your** inbox (`user.email` / `email.default_recipient`), not the SMTP from-address.                                                      |
| **9.4** | Send a text to a contact. Allow.                                                                                                                                                        |
| **9.5** | A text arrives: notify pill (and a chat tile if that number is in contacts). A number that is **not** in the book cannot open a chat until you add them — that is intended, not a fail. |
| **9.6** | SMS tiles do not persist across Quit.                                                                                                                                                   |
| **9.7** | “Did anyone text me?” → inbound_sms, not a web search.                                                                                                                                  |


### 10 Calendar


| Id              | What it is                                              |
| --------------- | ------------------------------------------------------- |
| **10.1**        | Read today’s / upcoming events.                         |
| **10.2 / 10.3** | Create one event, then delete it. One event, then gone. |


### 11 Schedule


| Id              | What it is                                                                                                                                                                                                       |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **11.1**        | “Every day at 7am, email me a summary of the weather.” Allow for `schedule`, not `send_email`. Job id in `[square-brackets]`. Hand run mailed a short note. Ask **What scheduled jobs do I have?** for the list. |
| **11.2 / 11.3** | Cancel that job. List empty / that id gone. Task Scheduler `\Arelis\` no longer has it.                                                                                                                          |


### 13 Rooms


| Id        | What it is                                                                                                        |
| --------- | ----------------------------------------------------------------------------------------------------------------- |
| **13.1**  | `/rooms` lists rooms.                                                                                             |
| **13.2**  | `/room new …` creates one.                                                                                        |
| **13.5**  | Leave and return.                                                                                                 |
| **13.6**  | Speech enter / leave (“go to …”, “leave the room”).                                                               |
| **13.7**  | A second room. Switch between two without mixing their threads.                                                   |
| **13.8**  | Leave button on the room strip. Leaves the room; does not delete it.                                              |
| **13.9**  | “Make me a room for astrophysics.” Allow, `rooms` tool, created. She does not walk you in. Then `/room forget …`. |
| **13.10** | `/room forget` removes it.                                                                                        |
| **13 UI** | Room purpose/root lines do not bleed into the composer.                                                           |


### 14 Presence / lifecycle


| Id       | What it is                                                                                                                                                              |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **14.1** | Notify pill / visual cue when something arrives.                                                                                                                        |
| **14.2** | Status pills (model / systems / notify).                                                                                                                                |
| **14.3** | Close with **X** goes to tray, not quit.                                                                                                                                |
| **14.5** | No black square on launch.                                                                                                                                              |
| **14.6** | From tray, double-click **Arelis (dev)** once. Existing window comes forward. No second window.                                                                         |
| **14.7** | Ghosting gone (frameless window no longer leaves a smear).                                                                                                              |
| **14.8** | Maximize, restore, resize from an edge. Does not stick unmaximized.                                                                                                     |
| **15.1** | Quit from the tray. Instant. `logs\arelis.log` has `quit_end_ms=…`, no `Task was destroyed but it is pending`. Two Python processes while running, **zero** after Quit. |
| **15.2** | History / rooms / layout survive relaunch.                                                                                                                              |
| **15.3** | Docks come back where you left them.                                                                                                                                    |


---

## Later — not this sitting, not a packing blocker

These were skipped or deferred. Each line is the actual check, so it is not a
mystery later.


| Id           | What to do                                                                                                                                                                    |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1.2**      | First-run dialog: “Choose the folder Arelis may work in.” Glass, not a grey Windows box. **Start Arelis** grants that folder. Only shows on a true first run (no roots yet).  |
| **2.2**      | Idle **TRY** chips (opening suggestions). Click fills the composer. Does **not** send.                                                                                        |
| **2.3**      | A no-tools ask (“how are you?”). She answers in prose. No weather / web_search / SMS.                                                                                         |
| **5.8**      | Workspace editor: type unsaved edits, then ask her to write that file. Chat says her write is on disk and she left your buffer alone. Open the file again to see her version. |
| **8.5**      | `Ctrl+Shift+M` may be swallowed by AMD Adrenaline. Rebind there, or pick another chord. Wake still starts conversation.                                                       |
| **14.4**     | Readiness strip copy when a poller is down (mail / SMS / Ollama). Says so once, not a spam of the same line.                                                                  |
| **15.4**     | `python -m arelis --cli`. Prompt `You>`. `/help` works. Ctrl+C exits.                                                                                                         |
| **3.4 note** | Test Mic names the device. Test Speak still says “airelyse”.                                                                                                                  |
| **9B walk**  | Full capability pass on `qwen3.5:9b` (the table above was 7B/14B). At least: orbit on cold launch, thinking as a paragraph, voice inbox, scrape/news with no JSON-fallback line after a good tool. |
| **chip**     | Consecutive same-tag turns should not flash `model loading… (fast:qwen3.5:9b)` as if weights were swapping.                                                                   |
| **ticker**   | Company / ticker exactness (training cutoff vs scrape quote) — optional, not the JSON-fallback hole.                                                                          |


---

*First walk 17 Aug 2026 (v0.2.1). Ledger rewritten 18 Aug 2026 so ids still
mean something. 0.2.2 packed the same day. Night: 9B overlay, code role
dropped, orbit/thinking/STT/JSON-fallback polish — still not a release.*